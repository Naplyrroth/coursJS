let array = ["mangue", "fraise", "pomme", "poire", "kiwi", "citron", "pêche", "mûre", "banane"];

let fruit = "pêche"; //Elément à rechercher

for(let i=0; i<array.length; i++) {
	if(fruit === array[i]) {
		console.log('Element Found');
    } 
}